# دليل إعداد Subdomain على GoDaddy

## 📋 الخطوات الكاملة

### ✅ المعلومات المطلوبة:
- الدومين: `xgym.website` ✅
- Subdomain المطلوب: `system.xgym.website`
- IP العام: (هتعرفه من الخطوة الأولى)
- Port التطبيق: `4001`

---

## 🔍 الخطوة 1: معرفة IP العام بتاعك

افتح: https://whatismyipaddress.com

احفظ الـ IPv4 اللي هيظهر لك (مثال: `197.123.45.67`)

---

## 🌐 الخطوة 2: إعداد Subdomain في GoDaddy

### الطريقة الأولى: استخدام GoDaddy فقط (HTTP فقط - بدون HTTPS)

1. ادخل على https://godaddy.com وسجل دخول
2. اضغط على **My Products**
3. جنب `xgym.website` اضغط **DNS**
4. في قسم **DNS Records**، اضغط **Add**
5. املأ البيانات:
   ```
   Type: A
   Name: system
   Value: (IP العام بتاعك)
   TTL: 600 seconds (أو Custom: 1 Hour)
   ```
6. اضغط **Save**

**⚠️ مشكلة:** GoDaddy مش بيدي SSL مجاني، يعني مش هيكون فيه HTTPS!

---

## 🔐 الخطوة 2 (البديل): استخدام Cloudflare للـ HTTPS المجاني (موصى به ✅)

### أ. إعداد Cloudflare:

1. سجل حساب مجاني على https://cloudflare.com
2. اضغط **Add a Site**
3. اكتب `xgym.website` واضغط **Add Site**
4. اختر **Free Plan** → **Continue**
5. Cloudflare هيفحص DNS Records الحالية
6. اضغط **Continue**

### ب. تغيير Nameservers في GoDaddy:

Cloudflare هيديك nameservers، مثلاً:
```
alex.ns.cloudflare.com
ruth.ns.cloudflare.com
```

**في GoDaddy:**
1. My Products → Domains → `xgym.website`
2. اضغط على الـ 3 نقاط → **Manage DNS**
3. تحت **Nameservers** اضغط **Change**
4. اختر **Custom**
5. احذف الـ nameservers القديمة
6. أضف الـ nameservers من Cloudflare
7. **Save**

انتظر 5-30 دقيقة

### ج. إضافة Subdomain في Cloudflare:

1. في Cloudflare → اذهب لـ **DNS** → **Records**
2. اضغط **Add record**
3. املأ:
   ```
   Type: A
   Name: system
   IPv4 address: (IP العام بتاعك)
   Proxy status: ✅ Proxied (سحابة برتقالي)
   TTL: Auto
   ```
4. **Save**

### د. تفعيل SSL:

1. في Cloudflare → **SSL/TLS**
2. اختر **Flexible**
3. **Save**

✅ دلوقتي عندك HTTPS مجاني!

---

## 🔧 الخطوة 3: Port Forwarding على الراوتر

### أ. معرفة IP جهازك المحلي:

في Command Prompt:
```cmd
ipconfig
```

دور على `IPv4 Address` تحت الشبكة النشطة (مثلاً: `192.168.1.100`)

### ب. إعداد Port Forwarding:

1. افتح المتصفح واكتب عنوان الراوتر:
   - `192.168.1.1` أو
   - `192.168.0.1` أو
   - `192.168.100.1`

2. سجل دخول (اليوزر والباسورد عادة على ظهر الراوتر)

3. دور على قسم اسمه:
   - **Port Forwarding** أو
   - **Virtual Server** أو
   - **NAT** أو
   - **Applications & Gaming**

4. أضف قاعدة جديدة:
   ```
   Service Name: Gym System
   External Port Start: 80
   External Port End: 80
   Internal Port: 4001
   Internal IP: 192.168.1.100 (IP جهازك)
   Protocol: TCP
   Enabled: ✅
   ```

5. **Save/Apply**

6. **اختياري (للـ HTTPS):** كرر نفس الخطوة لـ Port 443:
   ```
   External Port: 443
   Internal Port: 4001
   ```

---

## 💻 الخطوة 4: إعداد التطبيق

### أ. تحديث `.env`:

افتح ملف `.env` وغيّر:

```env
# Environment
NODE_ENV=production

# Public URL - بعد ما تخلص الإعداد
NEXT_PUBLIC_APP_URL=https://system.xgym.website

# JWT Secret - غيّره لحاجة قوية!
JWT_SECRET=your-super-secret-random-key-here-123456789

# Port
PORT=4001
HOSTNAME=0.0.0.0
```

### ب. Build التطبيق:

```bash
npm run build
```

### ج. تشغيل Production:

```bash
# الطريقة السريعة:
npm start

# أو باستخدام PM2 (أفضل):
npm install -g pm2
pm2 start npm --name "gym-system" -- start
pm2 save
```

---

## ✅ الخطوة 5: الاختبار

### أ. اختبار محلي:
```
http://localhost:4001
```

### ب. اختبار من الشبكة المحلية (موبايل على نفس الواي فاي):
```
http://192.168.1.100:4001
```

### ج. اختبار من الإنترنت:

بعد 5-30 دقيقة من تغيير DNS:
```
https://system.xgym.website
```

---

## 🔍 حل المشاكل

### المشكلة: الموقع مش بيفتح من برا

✅ **الحلول:**
1. تأكد إن Port Forwarding شغال (جرّب من موبايل على 4G)
2. تأكد إن Windows Firewall سامح Port 4001:
   ```cmd
   netsh advfirewall firewall add rule name="Gym System" dir=in action=allow protocol=TCP localport=4001
   ```
3. تأكد إن التطبيق شغال:
   ```cmd
   pm2 status
   # أو
   netstat -ano | findstr :4001
   ```

### المشكلة: DNS Error أو Can't resolve

✅ **الحل:**
- انتظر 30 دقيقة للـ DNS propagation
- جرّب من متصفح تاني أو Incognito
- امسح DNS Cache:
  ```cmd
  ipconfig /flushdns
  ```

### المشكلة: SSL Error (في Cloudflare)

✅ **الحل:**
- تأكد إن SSL mode على **Flexible**
- انتظر 5 دقائق بعد تفعيل SSL

---

## 📱 PWA - تثبيت على الموبايل

بعد ما كل حاجة تشتغل:
1. افتح `https://system.xgym.website` من موبايل
2. هتظهر رسالة "ثبت التطبيق"
3. اضغط "تثبيت الآن"
4. التطبيق يظهر على الشاشة الرئيسية! 📱

---

## 🎯 خلاصة الإعداد

| الخطوة | الوقت المتوقع | الحالة |
|--------|---------------|---------|
| معرفة IP العام | دقيقة | ⬜ |
| إعداد Cloudflare | 10 دقائق | ⬜ |
| تغيير Nameservers | 5 دقائق | ⬜ |
| Port Forwarding | 5 دقائق | ⬜ |
| تحديث `.env` | دقيقة | ⬜ |
| Build التطبيق | 2-5 دقائق | ⬜ |
| تشغيل Production | دقيقة | ⬜ |
| الاختبار | 5 دقائق | ⬜ |

**إجمالي الوقت: 30-60 دقيقة** ✅

---

## 📞 جاهز؟

بعد الانتهاء، النظام يكون متاح على:
- 🌐 **الرابط:** https://system.xgym.website
- 📱 **PWA:** قابل للتثبيت على الموبايل
- 🔒 **الأمان:** HTTPS مع Cloudflare
- ⚡ **السرعة:** CDN من Cloudflare

حظ سعيد! 🚀
