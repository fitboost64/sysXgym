# 🚀 بوابة الأعضاء - داخل النظام الأساسي

## 📍 الموقع الجديد

```
C:\Users\amran\Desktop\x gym\client-portal\
```

البوابة **دلوقتي جوا** ملفات النظام الأساسي! ✅

---

## 🎯 الهيكل

```
x gym/
├── app/              ← النظام الإداري (system.xgym.website)
├── prisma/
│   └── gym.db        ← قاعدة البيانات المشتركة
├── client-portal/    ← بوابة الأعضاء (client.xgym.website) ✨
│   ├── app/
│   ├── lib/
│   ├── prisma/
│   └── package.json
└── package.json
```

---

## 🚀 التشغيل المحلي

### الخطوة 1: تثبيت المكتبات

```bash
cd "C:\Users\amran\Desktop\x gym\client-portal"
npm install
```

### الخطوة 2: إنشاء Prisma Client

```bash
npx prisma generate
```

### الخطوة 3: تشغيل السيرفر

```bash
npm run dev
```

✅ **يعمل على**: http://localhost:3002

---

## 🌐 النشر على الإنترنت

### النظامين على نفس السيرفر

```
السيرفر
├── system.xgym.website  (بورت 4001) → النظام الإداري
└── client.xgym.website  (بورت 3002) → بوابة الأعضاء
```

كلاهما يقرأ من نفس قاعدة البيانات: `prisma/gym.db`

---

## 🔗 قاعدة البيانات

البوابة تقرأ من قاعدة بيانات النظام الأساسي:

```env
DATABASE_URL="file:../prisma/gym.db"
```

يعني:
```
x gym/
├── prisma/gym.db          ← القاعدة الأساسية
└── client-portal/
    └── يقرأ من القاعدة أعلاه
```

---

## ✅ المميزات

### 1. نفس المجلد
- ✅ كل الملفات في مكان واحد
- ✅ سهل الإدارة والصيانة
- ✅ نسخة احتياطية واحدة

### 2. دومينات مختلفة
- ✅ `system.xgym.website` → للإدارة
- ✅ `client.xgym.website` → للأعضاء

### 3. قاعدة بيانات واحدة
- ✅ بيانات متزامنة فوراً
- ✅ لا حاجة لـ APIs
- ✅ أسرع في الأداء

---

## 🛠️ الأوامر المفيدة

```bash
# تشغيل البوابة
cd client-portal
npm run dev

# عرض قاعدة البيانات
npx prisma studio

# بناء للإنتاج
npm run build
npm start
```

---

## 📚 الوثائق

- **[DEPLOYMENT_SUBDOMAIN.md](./DEPLOYMENT_SUBDOMAIN.md)** - دليل النشر الكامل
- **المجلد السابق**: `C:\Users\amran\Desktop\xgym-client-portal\`
  - لسه موجود وفيه كل الوثائق التفصيلية

---

## 🔄 الفرق عن السابق

| قبل | بعد |
|-----|-----|
| مجلد منفصل تماماً | جوا مجلد النظام الأساسي |
| بورت 3001 | بورت 3002 |
| `file:../x gym/prisma/gym.db` | `file:../prisma/gym.db` |

---

## 📊 البورتات

```
النظام الأساسي:     http://localhost:4001
بوابة الأعضاء:      http://localhost:3002
```

---

## 🎉 تم!

الآن عندك:
- ✅ البوابة **جوا** النظام الأساسي
- ✅ هتشتغل على **دومين مختلف**
- ✅ **نفس** قاعدة البيانات
- ✅ كل شيء **في مكان واحد**

**جرب دلوقتي:**
```bash
cd "C:\Users\amran\Desktop\x gym\client-portal"
npm install
npx prisma generate
npm run dev
```

ثم افتح: http://localhost:3002 🚀
